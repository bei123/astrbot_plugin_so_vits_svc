# 抖音音频下载器依赖包

# 核心依赖
aiohttp>=3.8.0          # 异步HTTP客户端
aiofiles>=23.0.0        # 异步文件操作

# 可选依赖（如果使用现有爬虫模块）
httpx>=0.24.0           # HTTP客户端（用于base_crawler）
pydantic>=1.10.0        # 数据验证（用于models）
browser-cookie3>=0.19.0 # 浏览器cookie提取（用于utils）

# 开发依赖（可选）
pytest>=7.0.0           # 测试框架
pytest-asyncio>=0.21.0  # 异步测试支持
black>=23.0.0           # 代码格式化
flake8>=6.0.0           # 代码检查
